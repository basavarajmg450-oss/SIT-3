{
  "name": "placementpro",
  "private": true,
  "description": "PlacementPro - Campus Placement Management",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --prefix placementpro-backend",
    "dev:frontend": "npm run dev --prefix placementpro-frontend",
    "install:all": "npm install && npm install --prefix placementpro-backend && npm install --prefix placementpro-frontend",
    "seed": "npm run seed --prefix placementpro-backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
